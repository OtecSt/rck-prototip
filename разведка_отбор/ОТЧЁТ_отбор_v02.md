# Отчёт Строителя_Отбора_2 — «Отбор» v0.2 (06.08.2026)

## БЛОК А. Трек ФЦК (Приложение № 4)

- **Извлечение:** Прил. № 4 (стр. 23–24 PDF) извлечено pypdf полностью, текстовый слой есть. Стр. 26 — скан без текстового слоя (139 символов колонтитула), но она относится к Прил. № 5 (РЦК), на полноту ФЦК-выписки не влияет.
- **Дословная выпись:** `разведка_отбор/ЧЕКЛИСТ_ФЦК_прил4.md`.
- **Структура чек-листа ФЦК:** 28 пунктов / 28 баллов (7+1+1+1+4+13+1), отсекающие «*»: 1.1, 1.2, 1.3, 1.5, 3 (5 шт.). Порог единый — 18 (п. 3.7).
- **Расхождения с РЦК (зафиксированы в выписке и спеке):** +п. 1.1 (стартовое совещание на модельном предприятии, 2 дня)*; 1.5* — отчётность ФЦК ежегодно 3 года; раздел 6 — 13 подпунктов вместо 16; **п. 4 в Прил. № 4 БЕЗ «*»** (в РЦК — отсекающий); **п. 5.4 без условно-отсекающей оговорки**; 5.3 — «план производства и план продаж»; п. 7 — соглашение «ФЦК-Предприятие».
- **Реализация:** `otbor.py` v0.2 — СПЕКИ_ТРЕКОВ {рцк, фцк}, валидация пунктов по треку, скоринг-карта и вердикт с указанием трека («кандидат РЦК/ФЦК»). `/api/otbor/skelet` отдаёт `спеки` обоих треков (старые поля сохранены для совместимости). UI: переключатель «Трек очного отбора» в карточке Отбора; смена трека перерисовывает чек-лист и сбрасывает его ответы (чек-листы разные); живой счёт по максимуму трека.

## БЛОК Б. Помощник выбора пилотного потока

- **Ядро:** новый файл `otbor_potok.py` (CLI + validirovat/raschet/скоринг_карта). Критерии потока — 5 × 1 балл (доля выручки ≥ 10 % — **обязательный**, ответ «нет» → кандидат не рекомендуется; цикл ≤ 1 мес.; потенциал роста продаж; план производства и продаж; самостоятельность в сбыте). Карта признаков потерь — **15 признаков** (пп. 6.2–6.16 Прил. № 5 дословно; 6.1 исключён — он критерий цикла, двойного счёта нет). Итог кандидата — макс 20.
- **Рекомендация:** макс итог среди допущенных; тай-брейк — баллы критериев, порядок ввода; обоснование с разрывом от ближайшего и списком дисквалифицированных.
- **Сервер:** `/api/otbor/potok/skelet`, `/api/otbor/potok`; прогоны типа `otbor_potok` по контракту (immutable, сервер пересчитывает через `sчитать_itogi`, сравнение `sravnit_potok` + дельта-md).
- **UI:** отдельная под-карточка «Выбор пилотного потока» в этапе «Предстарт» (не раздувая карточку Отбора): 1–3 кандидата («+ кандидатный поток»), подсказки у критериев, живой скоринг и предварительная рекомендация, скоринг-карта md (сервер + скачивание), сохранение прогона, история + сравнение.
- **Dockerfile:** добавлен `otbor_potok.py` в COPY (проверено на бое).

## Эталонные прогоны (локально и на бое — совпали)

- ФЦК: 28 «да» → **28/28, «проходит», трек фцк**; 17 баллов без отсекающих → **«не проходит» (порог не пройден)**; «нет» по 1.1 → «отклонено», отсекающие [1.1].
- Поток, эталон 1 (2 кандидата): **«рекомендован пилотный поток: Линия фасовки»** — итог 14/20 (критерии 5/5, признаки 9/15) vs «Участок грануляции» 7/20 (разрыв 7 баллов). Эталон 2: кандидат «Склад ГП» с долей < 10 % дисквалифицирован, рекомендована «Линия фасовки» (10/20). Признаки потерь считаются (9/15, 3/15, 5/15).
- Прогоны `otbor_potok`: сохранение № 45/46, сравнение «до/после» — дельта-md корректен (тесты удалены).

## Регрессия (локально + бой)

- УМ ОКЗ слепой: **38,7 %** ✓; ЭЭ НОВОХРОМ: **23 757 960,67** ✓; vsm (svg) ✓; SMART 200 ✓; ОРД docx ✓; протокол skelet 200 ✓; ee_export_fck xlsm ✓.
- otbor v0.1 эталоны РЦК (CLI): 30/проходит, 17/не_проходит, отсекающий п. 4 → отклонено, 5.4 условный — PASS.

## БД и серверы

- Тестовые прогоны (45, 46) зачищены; дамп до/после идентичен (расхождение только счётчик sqlite_sequence — содержимое прогонов и проектов неизменно).
- Локальный сервер перезапущен и **оставлен запущенным** (PID 38863, порт 8000, AGENT_CEH_PASSWORD задан).
- **Бой задеплоен:** rsync + `docker compose build app && up -d`, контейнер healthy. Проверки с Mac curl -k: / 200 (новая карточка на странице), /api/otbor (треки, ФЦК 28 и 17), /api/otbor/potok (skelet + эталон), регрессия УМ и ЭЭ — всё PASS.

## Документация

- Помощь (index.html): 5.0.1–5.0.6 (трек ФЦК, выбор потока), строка сводной таблицы, п. 9.7, глоссарий +3 термина («Пилотный поток», «Отсекающий критерий», «Скоринг-карта»).
- МЕТОДИЧКА_пользователя.md: таблица 5.0.1 +2 строки, новый короткий раздел 5.0 (5.0.1–5.0.3), п. 9.7; PDF пересобран.

## Что не вошло / замечания

- Стр. 26 PDF — скан без текстового слоя (к Прил. № 4 отношения не имеет; зафиксировано в выписке).
- «Ранжирование потоков (протокол выбора)» и «форма № 4» остались в dev-line «В разработке» — за рамками задания.
- Сравнение прогонов `otbor` по-прежнему помечает частичным сравнение разных треков (РЦК vs ФЦК) — это осознанно.
