# AGENTS.md — «Ведение проектов · РЦК» (rck-prototip)

> Конституция проекта для ИИ-агентов. Обязательна к исполнению в каждой сессии.
> Живые документы (протоколы решений, стандарт бережливой работы, перенос контекста)
> лежат каталогом выше: `../ПРОТОКОЛ_совещаний.md`, `../СТАНДАРТ_бережливая_работа.md`,
> `../ПЕРЕНОС_КОНТЕКСТА_0609.txt` — читать при доступности. Этот файл самодостаточен
> по правилам; изменения в него — только решением оператора, через протокол.

## 1. Что это за проект

Система ведения проектов по бережливому производству и производительности труда
(методологии ФЦК — Федерального центра компетенций). Веб-приложение (Flask) + расчётное
ядро + генераторы документов. Оператор — Александр (инженер, ФЦК). Язык проекта,
UI и ответов — русский.

**Критично:** «ЭЭ» в этом проекте = **экономический эффект** по методике МР-13-2025
(ред. 4), НЕ энергоэффективность. Энергоаудита и 275-ФЗ в проекте нет.

Домены: VSM-картирование, показатели потока (такт, ВПП), узкие места, OEE,
цифровой замер, мероприятия, ОРД/SMART, экономический эффект (ЭЭ), документы ФЦК,
протоколы закрытия, презентации «Защита одной кнопкой», симулятор потока.

## 2. Карта репозитория (факт, снят 07.09.2026)

Корень — расчётное ядро и генераторы; `ui/` — веб-приложение; `deploy/` — Docker.

| Модуль | Размер | Назначение |
|---|---|---|
| `ui/app.py` | 173K | Flask-сервер, API, маршруты, вся склейка вкладок |
| `dokumenty_fck.py` | 97K | генерация документов ФЦК (DOCX/PDF) |
| `meropriyatiya.py` | 91K | генератор и управление мероприятиями |
| `protokol_zakrytiya.py` | 67K | протокол закрытия проекта |
| `ээ_калькулятор.py` | 48K | ядро ЭЭ v0.2 на МР-13-2025 ред. 4 (налог 25 %, цена «после», сверхурочные FTE, отложенный эффект запасов, J-декомпозиция) |
| `otbor.py` / `otbor_potok.py` | 47K / 24K | отбор проблемы и потока |
| `ord_smart.py` | 41K | ОРД и SMART-формулировки задач |
| `llm_sloy.py` | 32K | LLM-слой: только тексты/подсказки, НЕ расчёты |
| `vr_export.py` | 31K | экспорт визуального ряда |
| `ui/preza_pptx.py` / `ui/preza_data.py` | 28K / 20K | «Защита одной кнопкой»: PPTX-движок и сборщик данных |
| `biblioteka_instrumentov.py` | 28K | библиотека инструментов БП (427 практик, топ-100) |
| `ui/simulyator.py` | 27K | симулятор потока (сценарии, дельты) |
| `ekstraktor_pa.py` | 24K | экстракция данных ПА |
| `cifrovoy_zamer.py` | 23K | цифровой замер |
| `oee_kalkulator.py` | 23K | OEE = A × P × Q |
| `vsm_karta.py` | 22K | VSM-карта |
| `узкие_места.py` | 20K | узкие места |
| `ээ_экспорт_фцк.py` | 20K | справка ФЦК об ЭЭ — заполнение шаблона .xlsm (openpyxl, keep_vba=True) |
| `pokazateli_potoka.py` | 19K | такт, ВПП, время цикла |

**Данные.** Исходники предприятий — JSON в корне (`данные_ээ_*.json`, `данные_окз_слепой.json`,
`данные_умд.json`, `данные_протокол_кушкуль.json`): для кода read-only, правка — только
явным скриптом импорта с согласования оператора. Рабочие проекты и прогоны — SQLite
`ui/agent_ceh.db` (в git НЕ входит, бэкапы `*.bak_*` — тоже).

**Тесты** — 17 файлов `_test_*.py` + `ui/_qa_*.py` (CDP-прогоны UI).
**Прогоны** — `прогон_*.md`: сценарии/промпт-шаблоны LLM-агентов.
**Методичка** — `МЕТОДИЧКА_пользователя.md` (~87K) + PDF ред. 3.0/3.1.

## 3. Среда и запуск

- Python: только `.venv/bin/python` (3.13.x). Системный python3 без зависимостей — не использовать.
- Создание среды: `python3.13 -m venv .venv && .venv/bin/pip install -r deploy/requirements.txt pypdfium2`
- Локальный запуск: `cd ui && ../.venv/bin/python app.py` (порт 8000 может быть занят чужим dev-сервером — не трогать, брать свой порт, напр. 8012).
- Оснастка машины: LibreOffice (рендер-приёмка PPTX), node v24 + Chrome (CDP-прогоны).

## 4. Гейты качества (обязательны)

1. **Любое изменение кода = `./run_all_regress.sh`.** Ожидание: **108/108**
   (sim_s1 8, sim_s2 25, preza_p2 20, preza_p3 14, ee_e1 26, fck64 15; fck64 сам
   поднимает сервер на порту 8012 и гасит его). Красный прогон — стоп, рапорт, план.
2. **Правки расчётов = сверка с эталонами** (раздел 5) + обоснование в отчёте.
3. **UI-фичи** принимаются CDP-прогоном по образцу `ui/_qa_c22_ui.py`, `ui/_qa_e1b_ui.py` —
   не «на глаз».
4. **Вердикт первой строкой** каждого отчёта: PASS / PASS_WITH_CAVEAT / PARTIAL / FAIL /
   CRITICAL_STOP. PARTIAL и FAIL — легитимные исходы.
5. **Эскалация (джидока):** блокер — стоп и рапорт оператору, не импровизировать.
6. **PPTX** — рендер-приёмка через LibreOffice (наложения, переполнения) до сдачи.

## 5. Эталоны расчётов (золотой стандарт, не менять без решения оператора)

- УМ ОКЗ фасовка: **38,7 % ВПП**
- ЭЭ НОВОХРОМ (ред. 2, архивная справка): **23 757 960,67 ₽**
- Такт **2,00** / ВПП **7000,00** (демо-поток)
- OEE **0,704167** (75 % × 97 % × 97 %)
- ЭЭ-модель v5.23 «Пример расчета»: ЭЭреал **11 261 156,71** / ЭЭпот **2 010 025,66** /
  высвобождение **705 000** / отложенный **42 300** / налоги 3 года **13 596 144,77**
- Решение 06.09: множитель (1−Б) к объёму «до» в v5.23 **не применяется** (ломает
  архивные эталоны); пересмотр — только по живой справке v5.23 от ФЦК.

## 6. Жёсткие запреты

1. **Никакого LLM в расчётах** — только детерминированный код. LLM — тексты и подсказки.
2. **Данные ВКР (поток 100У) — не использовать нигде.** Чужой материал.
3. Без англицизмов и эмодзи в UI (тонкие SVG — можно).
4. Excel — macro-free: новых макросов не создавать; шаблоны ФЦК .xlsm заполнять
   через openpyxl с keep_vba=True.
5. Не добавлять зависимости без спроса; новые модули — сразу в COPY-список
   `deploy/Dockerfile` (прецедент Б2: справка ФЦК не попала в образ).
6. Не оставлять запущенных серверов, временных файлов, «хвостов» процессов (5С).
7. Не переписывать JSON-данные предприятий; не удалять существующие тесты без
   явного разрешения.
8. Пароли и секреты — не писать в файлы и отчёты. Пароль прода = AGENT_CEH_PASSWORD
   в `/opt/agent-ceh/deploy/docker-compose.yml` на сервере.

## 7. Безопасность и контакт с оператором

1. **Постоянный контакт:** перед каждым необратимым или внешне видимым действием —
   явное подтверждение оператора: удаление файлов/данных, правки БД, деплой на прод,
   push с перезаписью истории, массовые переименования.
2. Правки кода — показывать diff и ждать «давай», если правка выходит за согласованный
   ломтик.
3. Прод (https://201.34.132.154) — только по команде оператора, строго по деплой-ритуалу
   (раздел 8).
4. Один агент = один ломтик. Продолжение — resume того же агента, не новый заход.
5. Ревизия: по слову оператора «ревизия» или раз в 3 ломтика — вход только артефакты
   (протоколы, метрики), выход — узкие места, FPY, рекомендации.

## 8. Git и деплой-ритуал

Три эшелона сохранности: локальный git (источник истины) → GitHub private
`github.com/OtecSt/rck-prototip` (main, SSH) → git-бандл на VPS `/opt/agent-ceh/backups/git/`.

1. Коммит перед каждым деплоем; хеш — в отчёт/протокол.
2. Деплой: `rsync -a ui/ root@201.34.132.154:/opt/agent-ceh/ui/` (+ модули корня по
   схеме Dockerfile) → `cd /opt/agent-ceh/deploy && docker compose build app &&
   docker compose up -d --force-recreate app` → проверка: HTTP 200, маркеры фичи в HTML,
   CDP по продy для рискованных ломтиков.
3. Прод ≡ локаль: при подозрениях — сверка md5 ключевых файлов.

## 9. Регистр работы

- Ответ оператору: **Статус → Факт → Решение → Риск → Следующий шаг.** Без лести,
  без «давайте попробуем».
- Разделять: подтверждённые данные (из JSON/БД) / аналитические выводы (расчёты) /
  проектные предложения / ожидаемые эффекты (экономика).
- Любая формула — с обоснованием и проверкой единиц измерения. Видишь пробел,
  несоответствие, потенциальный баг — флажь сразу.
- После каждой задачи предлагать следующий шаг: вариант A / B / C.

## 10. Типичные ловушки (прецеденты)

- Ключ истории симуляции — `simulation`, не `sim`.
- Новые файлы, нужны на проде → Dockerfile COPY, иначе 500 на проде.
- `WWW-Authenticate` realm кириллицей: werkzeug dev даёт 500 вместо 401 (на проде gunicorn — ок).
- Эталоны ред. 2 (архивные справки) и ред. 4 (v5.23) живут параллельно — не «чинить»
  расхождение, это режим совместимости.

## 11. Материалы «Методички» — обязательны к использованию

Проект живёт внутри библиотеки методик ФЦК: репозиторий = `Методички/ИИ-направление/Прототип/`.
При разработке любой фичи СНАЧАЛА ищи методику-источник в этих папках — не выдумывай
предметную область. Разведка делается один раз и складируется в выписку (образец —
`../разведка_ээ_0409/`); оригиналы повторно не читаются.

Пути даны от корня репозитория (`Прототип/`): `../` = ИИ-направление, `../../` = Методички.

Живые документы проекта (`../`):
- `../ПРОТОКОЛ_совещаний.md` — все решения (последний № 64); `../СТАНДАРТ_бережливая_работа.md` — правила цеха;
  `../ПЕРЕНОС_КОНТЕКСТА_0609.txt` — состояние и очередь; `../Спеки/` — спеки и стандарт спек;
  `../DESIGN_V2.md` — дизайн-концепция; `../Архитектура.md`, `../Видение.md`.

Разведки и корпуса (`../`):
- `../разведка_ээ_0409/` — выписки по МР-13-2025 ред. 4 и моделям v5.23 (источник истины для серии Э);
- `../корпус_практик_фцк/` — 427 практик, топ-100 (306 файлов); `../корпус_разборов/`;
- `../проеткы/` + `../проеткы_карта.md` — реальные проекты, демо-кейсы Оренбив и Строгонов;
- `../входящие_фцк/`, `../образцы_па/`, `../разведка_па/`, `../аудит_0109/` — реестры аудита 01.09.

Методики ФЦК — источники истины (`../../`, тематические папки):
- `../../Оценка экономического эффекта от реализации мероприятий по ПППТ/Полный_перечень_материалов/` —
  МР ред. 4 (PDF), модель ЭЭ производственных процессов v5.23 (xlsm), схемы и расчёты, модель обеспечивающих v5.23;
- `../../Оценка экономического эффекта … обеспечивающих процессов …/` — МР + модель обеспечивающих процессов;
- тематические папки по инструментам: 5С, SMED («Быстрая переналадка»), OEE/ТРМ («Анализ эффективности
  оборудования», «Развертывание ТРМ», «Автономное обслуживание»), канбан, андон, адресное хранение,
  стандартизированная работа, TWI, VSM («Составление карт потоков и процессов», МУ-10-2026), запасы
  («Управление запасами», «Управление запасными частями»), SPC, «Решение проблем», «Инструменты
  декомпозиции целей», «Показатели ПППТ» и др.;
- `../../База знаний ФЦК/` — Obsidian-vault: формуляр показателей (01_Формуляр), конспекты методик, глоссарий;
- `../../Реестр_методик_ФЦК_актуальный_….xlsx` — актуальный реестр методик.

Запрет: `../вкр_находки/` — данные ВКР (поток 100У), не использовать нигде.
