# ФИКС_ИНЖЕНЕР_04 — закрытие ран УБИЙЦА_РЕВИЗИЯ_v01

> Дата: 03.08.2026. Стенд: `Прототип/ui/app.py` + ядра `узкие_места.py`,
> `ээ_калькулятор.py`, `protokol_zakrytiya.py`, `ord_smart.py` + `templates/index.html`.
> Стоп-линия соблюдена: только фиксы валидации/ошибок/безопасности, новых фич нет.
> Контракт расширен документированно: `ui/КОНТРАКТ_данных.md`, новый раздел **Р7** (ред. v1.2).

## Таблица закрытия: рана → фикс → доказательство

| # | Класс | Фикс | Доказательство закрытия |
|---|---|---|---|
| У-1 | CRITICAL | Строгий разбор тела `json.loads(parse_constant=raise)` + рекурсивный `math.isfinite` по входу во ВСЕХ расчётных API (`raschet`, `ee_raschet`, `progony`, `protokol`, `ord`, `smart`, `excel_extract`, `sravnenie`); валидаторы ядер (`_конечное` в app.validirovat, `proverit_vhod`, `ee_kalk._число`) требуют конечное число ≠ bool | `NaN` в `/api/raschet` → **[400]** «значение „NaN" — не конечное число»; `1e999` в тц_мин → **[400]** «поле „операции[1].тц_мин": не конечное число (inf от переполнения)»; `NaN` в `/api/progony` → **[400]**; `-1e999` в `/api/ee_raschet` → **[400]**. Ни один NaN не попадает в immutable-снапшот |
| У-3 | CRITICAL | После `ee_kalk.raschet` проверка конечности итогов (`ээ`, `ээ_пот`, `высвоб`, `налоги`) в `/api/ee_raschet` и в `sчитать_itogi` (снапшот); nan/inf → отказ | `цена_ед=1e308, время=1e308` → **[400]** «результат расчёта („ээ") — не конечное число (nan/inf): проверьте масштаб входных данных». «nan руб/год» в отчёте больше не печатается |
| У-2 | CRITICAL | `баллы_из_оценок`: оценка строго ∈ `ОЦЕНКИ_ТРЕБОВАНИЯ` («Соответствует»/«Не соответствует»/«Н/П», точные строки из скелета); иная строка → 400 с номером требования; действует в `POST /api/progony` (protokol) и `POST /api/protokol` | 87 оценок «АБРАКАДАБРА» → **[400] ok:false, 87 ошибок**, первая: «чек-лист, требование 1.1: недопустимая оценка „АБРАКАДАБРА" — допустимы только: „Соответствует", „Не соответствует", „Н/П"». 42 балла из мусора невозможны |
| У-4 | БЛОКЕР | `db()`: отказ, если файла БД нет (sqlite не создаёт пустышку); `@app.errorhandler(sqlite3.Error)` → JSON **503**; `@app.errorhandler(Exception)` → JSON 500 (страховка); `init_db`: `PRAGMA integrity_check` при старте, битая БД = `sys.exit` с диагнозом | `mv agent_ceh.db /tmp` → `GET /api/proekty` → **[503] JSON** «База данных недоступна: файл … не найден — сервис НЕ создаёт пустую базу»; **0-байтовая БД НЕ зародилась** (`ls` подтвердил); после возврата файла — [200]. Старт с мусором в файле БД → «СТОП: файл БД … не читается (file is not a database)…» — процесс не поднялся |
| У-5 | БЛОКЕР | `type(x) is int` (не isinstance) для `id_proekta`, `id_do`, `id_posle`; несуществующий проект — 404 как ранее | `{"id_proekta": true}` → **[400]** «должно быть целым числом > 0 (bool недопустим)»; `{"id_do": true}` в `/api/sravnenie` → **[400]**. Записи в проект № 1 не произошло |
| У-6 | ВАЖНО | Серверная сверка контекста в точке записи: предприятие/поток входа ≠ карточке проекта → `предупреждения` в ответе (не отказ) + `"контекст_расходится": true` в immutable-итогах | POST чужого потока «ТЕПЛИЦЫ КУШКУЛЬ» в проект ОКЗ → [200] с `предупреждения: ["Контекст входа расходится…"]`, в итогах `контекст_расходится: true`; UI показывает ⚠ в статусе сохранения (обе волны). Тест-прогон удалён |
| У-7 | ВАЖНО | Подтяжка ЭЭ: `max(ee_runs, key=(дата, метка=="после", id))` — самый СВЕЖИЙ по дате, «после» только тай-брейкер; источник без метки «после» → предупреждение; в `источники.*` добавлена `метка`; UI показывает дату и метку («без метки „после"») | Тест-проект: ee «после» (старый) + ee «до» (новее) → подтяжка вернула **свежий № 34** (ранее тащила старый «после») + предупреждение «подставлен замер без метки „после"…». Тест-данные удалены |
| У-8 | ВАЖНО | `/api/progony/<id>/файл`: `resolve()` + containment внутри `ui/файлы/` + шаблон имени `progon_<id>_(орд\|протокол).docx`, иначе 404 | `итоги_json.файл = "../результаты/СКВОЗНОЙ_ЭКЗАМЕН_v01.md"` (через sqlite) → **[404]** «файл … указывает вне каталога файлов сервиса — запись повреждена, файл не отдан». Содержимое НЕ отдано |
| У-9 | ВАЖНО | ЭЭ ≤ 0: предупреждение в ответе `POST /api/progony` (protokol) и в `отчёт_md`; в docx — жирная пометка «ВНИМАНИЕ…»; `X-AgentCeh-Warning` у `/api/protokol` (ASCII — кириллица в заголовке ломала запись сокета, найдено и исправлено при проверке); типографский минус «−500 000» в docx и md | `{"ээ_руб_год": -500000}` → [200], docx: «…составит **−500 000 руб. в год**.» + «**ВНИМАНИЕ: экономический эффект неположительный (−500 000 руб. в год). Проверьте входные данные перед подписанием протокола.**»; `ээ=0` в снапшот → предупреждение «ЭЭ ≤ 0…». Тест-прогон удалён |
| У-10 | ВАЖНО | `data_start` — try/int + отсечка `< 1`; глобальный `@app.errorhandler(Exception)` — любой сбой JSON, не HTML | `data_start:"abc"` → **[400] JSON** «должна быть целым числом ≥ 1»; `data_start:-50` → **[400] JSON** «нумерация строк/столбцов Excel начинается с 1». Валидное извлечение работает (`резка, тц=5.0`) |
| У-11 | ВАЖНО | Потолки: `вход_json` ≤ 5 МБ, операций ≤ 500, имя ≤ 300 символов → 400; текст 413 обновлён | 6000 операций (10,7 МБ) → **[400]** «операций 6000 больше потолка 500»; 501 операция → **[400]**; тело >20 МБ → **[413]** нейтральный текст |
| У-12 | ВАЖНО | `beforeunload` при «грязной» карточке (переиспользован флаг `формаКонтекст[key] !== undefined`) — в `index.html` | Код добавлен в слой данных (блок «Старт»): любой несохранённый ввод в любой карточке инструмента → браузерное предупреждение при F5/закрытии |
| У-14 | ЖЕЛАТ. | Текст 413 по `request.endpoint`: Excel-инструкция только у excel_preview, иначе нейтральная | См. У-11: **[413]** «запрос больше 20 МБ — уменьшите объём данных…» на `/api/progony` |

## Регрессия (полная, до копеек)

| Эталон | Ожидание | Факт | Сходится |
|---|---|---|---|
| ЭЭ НОВОХРОМ | 23 757 960,67 | 23 757 960,67 | ✅ |
| ЭЭ Строгонов | 11 480 819,11 | 11 480 819,11 | ✅ |
| ЭЭ Оренбив | 8 350 501,24 | 8 350 501,24 | ✅ |
| ЭЭ Атэна | 9 267 933,87 | 9 267 933,87 | ✅ |
| ОКЗ, доля ВПП главного УМ | 38,7 % («фасовка…», 2 детектора) | 38,7 % | ✅ |
| Кушкуль, чек-лист | 22 балла, порог пройден | 22, порог_пройден: True | ✅ |
| Экстрактор Excel | валидное извлечение | ok:true, `резка 5.0` | ✅ |
| `/api/sravnenie` (УМ 9 vs 11) | ok, частичное | ok:true, частичное:true | ✅ |
| Контекст-механика (Н3/Н4) | чужой ЭЭ/УМ не подставляются | оба отказа с внятными предупреждениями | ✅ |

## Завершение (состояние стенда)

- Сервер **остановлен**, `pgrep -f app.py` — **чист**.
- Тест-мусор удалён: прогоны № 31–34, проект № 7, docx-тестов не осталось
  (`ui/файлы/` — только боевые progon_4…22).
- БД: `PRAGMA integrity_check` = **ok**, `progony=22`, `proekty=4` — боевые
  проекты № 1–4 и прогоны № 1–22 не тронуты.
- Контракт: `ui/КОНТРАКТ_данных.md`, раздел **Р7** (ред. v1.2) — все изменения
  API документированы (расширение, обратная совместимость сохранена).

## Задел (не входило в этот заход)

1. **У-12 (продолжение)**: автосохранение черновика формы в localStorage
   (87 оценок чек-листа, Excel-разметка) — beforeunload есть, черновика нет; v1.3.
2. **У-13**: контракт §2 говорит «400 — нет проекта», факт 404. Оставлено 404
   (по заданию У-5 подтверждён 404); контракт переиздать или код привести — решить владельцу.
3. **У-15/У-16/У-17/У-18**: sessionStorage-проект, баннер порта/зомби-инстанс,
   предупреждение о микро-ЭЭ (<1 руб), localhost-токен доступа — желательные,
   не тронуты.
4. Найден при проверке и исправлено: кириллица в HTTP-заголовке `X-AgentCeh-Warning`
   рвала запись ответа (latin-1) — значение заголовка теперь ASCII.
